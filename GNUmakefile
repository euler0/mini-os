# Issue a useful error message if a user tries to build the project
# using GNU make.
# Inspired from the ELF Tool Chain project.

all:
	$(error ERROR: This source tree needs to be built with BSD 'make')
